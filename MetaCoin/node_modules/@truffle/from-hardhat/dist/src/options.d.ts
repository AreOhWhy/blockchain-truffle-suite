export interface EnvironmentOptions {
    /**
     * Working directory from which to interact with Hardhat
     *
     * @default process.cwd()
     */
    workingDirectory?: string;
}
export declare const withDefaultEnvironmentOptions: ({ workingDirectory }?: EnvironmentOptions) => EnvironmentOptions;
